---
title: "ubuntu20.04 react开发"
tags: [react, node]
categories: 工具 
---

```
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs
node -v
sudo corepack enable
sudo npm install -g yarn
yarn -v
```